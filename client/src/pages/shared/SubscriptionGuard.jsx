// client/src/components/shared/SubscriptionGuard.jsx
// Subscription redirects disabled so all users can access dashboard pages freely.

export default function SubscriptionGuard() {
  return null
}
